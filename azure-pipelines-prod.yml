name: prod-$(Date:yyyyMMdd)$(Rev:.r)

trigger: none

pr: none

variables:
  - group: databricks-prod

extends:
  template: .azuredevops/templates/deploy-databricks-bundle.yml
  parameters:
    targetName: prod
    environmentName: databricks-prod
