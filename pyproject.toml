[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "cs-databricks"
version = "0.0.0"
description = "cs-databricks"

[tool.setuptools]
packages = ["src"]
