name: 'Push to staging'

on:
  pull_request:
    branches:
      - staging
    types:
      - closed

concurrency:
  group: 'push-to-staging'
  cancel-in-progress: false

env:
  DATABRICKS_HOST: https://adb-7405615578261463.3.azuredatabricks.net
  ENVIRONMENT: staging
  LOG_ANALYTICS_WORKSPACE_ID: 7d0cb39c-348d-4863-91e1-efefc333fc11

jobs:
  deploy-to-staging:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install databricks-sdk requests

      - name: Run tests
        run: |
          pip install -r requirements-dev.txt
          pytest tests/ -v

      - name: Deploy notebooks to Databricks STAGING
        id: deploy
        env:
          DATABRICKS_PAT: ${{ secrets.DATABRICKS_STAGING_PAT }}
        run: |
          python .github/workflows/scripts/deploy-notebooks.py \
            --environment staging \
            --branch ${{ github.head_ref }} \
            --databricks-host ${{ env.DATABRICKS_HOST }} \
            --output-file deployment-result.json

      - name: Run health checks
        id: health-check
        env:
          DATABRICKS_PAT: ${{ secrets.DATABRICKS_STAGING_PAT }}
        run: |
          python .github/workflows/scripts/health-check.py \
            --environment staging \
            --databricks-host ${{ env.DATABRICKS_HOST }} \
            --timeout 300

      - name: Send deployment logs to Log Analytics
        if: always()
        env:
          LOG_ANALYTICS_WORKSPACE_KEY: ${{ secrets.LOG_ANALYTICS_WORKSPACE_KEY }}
        run: |
          python .github/workflows/scripts/send-logs-to-analytics.py \
            --workspace-id ${{ env.LOG_ANALYTICS_WORKSPACE_ID }} \
            --workspace-key ${{ env.LOG_ANALYTICS_WORKSPACE_KEY }} \
            --environment staging \
            --status "${{ job.status }}" \
            --deployment-result deployment-result.json

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `❌ **STAGING Deployment Failed**\n\nCheck workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`
            })
